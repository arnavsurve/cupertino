# TODO

## Publishing experience

- [x] `cupertino publish` — build tarball and upload in one command
- [x] `cupertino init` — scaffold a `package.json` interactively
- [x] `cupertino publish --dry-run` — validate before uploading

## Consumer experience

- [x] Default registry URL to `cupertino.sh` instead of `localhost:8080`
- [x] `cupertino search` — search for packages from the CLI
- [x] `cupertino info <package>` — inspect a package before installing
- [x] `cupertino upgrade [package]` — update installed packages

## Ecosystem

- [ ] Seed the registry with real packages
- [ ] Package namespacing / name squatting prevention
- [ ] Package signing
- [ ] Private registry support documentation
